import 'package:flutter/material.dart';

class StudyHistoryTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Study History'),
      ),
    );
  }
}