import 'package:flutter/material.dart';

class StudyAnalysisTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Study Analysis'),
      ),
    );
  }
}